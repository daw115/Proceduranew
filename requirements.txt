# Pełny zestaw zależności dla WSZYSTKICH skryptów w repo.
# Finalny build v7 (_build_procedura_final.py) wymaga tylko pakietów
# z requirements-final.txt (Markdown + Pillow, wersje pinowane).
#
# Zależność systemowa (poza pip): pandoc — wymagany przez _build_docx.py.

# Build v7 / final (pinowane — final weryfikuje dokładne wersje)
Markdown==3.10.2
Pillow==12.3.0

# Skrypty legacy i narzędzia pomocnicze
beautifulsoup4   # generate_full.py
python-docx      # _build_docx.py, _build_naklejki.py, generate_full.py
weasyprint       # _build_info_pdf.py, _build_procedure_pdf.py, generate_full.py
reportlab        # _build_naklejki.py
openpyxl         # _build_procedure.py, _build_statichints.py, _build_naklejki.py
pypdf            # _build_info_pdf.py, _build_procedure_pdf.py, _build_naklejki.py
fpdf2            # _gen_pdf.py
